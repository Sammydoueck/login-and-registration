# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Login and Registration' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Login and Registration
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  
  target 'Login and RegistrationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Login and RegistrationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
